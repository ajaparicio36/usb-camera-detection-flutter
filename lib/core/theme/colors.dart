import 'package:flutter/material.dart';

class AppColors {
  // Primary light blue color
  static const Color primaryBlue = Color(0xFF90CAF9); // Light Blue 200

  // Background color - a very light blue/grey for neumorphic effect
  static const Color background = Color(0xFFEEF5FD);

  // Shadow colors for neumorphic effect
  static const Color shadowDark = Color(0xFFCCD9E6);
  static const Color shadowLight = Color(0xFFFFFFFF);

  // Accent colors
  static const Color accentBlue = Color(0xFF64B5F6); // Light Blue 300
  static const Color textColor = Color(0xFF37474F); // Blue Grey 800
  static const Color iconColor = Color(0xFF546E7A); // Blue Grey 600
}
